const unlockPage = '.unlock-page';
const passwordInput = `${unlockPage} #password`;
const unlockButton = `${unlockPage} button`;
const importButton = `${unlockPage}__link--import`;
const secretWordsInput = '.import-account__input-wrapper input';
const passwordInputImport = '#password';
const confirmPasswordInputImport = '#confirm-password';
const restoreButton = '.btn--first-time';

module.exports.unlockPageElements = {
  unlockPage,
  passwordInput,
  unlockButton,
  importButton,
  secretWordsInput,
  passwordInputImport,
  confirmPasswordInputImport,
  restoreButton,
};
